{
  "$schema": "Each entry is a documented exception to the critical+serious axe contract. Add ONLY with a reason and an `expires` date — a stale allowlist is the same shape as the bug it's meant to hide.",
  "entries": []
}
